// ESTRATEGIA DE AUTH: "Hardcoded Dev Mode"
export const CURRENT_USER_ID = "profe_master_dev";

// FIREBASE CONFIGURATION
// NOTE: In a real production app, these would be in environment variables.
// Since this is a generated demo, we will use a LocalStorage Mock Service if these are empty.
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID"
};
